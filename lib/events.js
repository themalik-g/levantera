const ai = (function () {
  const k = {
    Nlhnc: function (o, p) {
      return o !== p;
    },
    zKXTF: "SqWsk",
    YQRZe: function (o, p) {
      return o === p;
    },
    OzbtY: "pmSsi",
  };
  const l = k;
  let m = true;
  return function (o, p) {
    if (l.YQRZe(l.OzbtY, l.OzbtY)) {
      const q = m
        ? function () {
            if (p) {
              if (l.Nlhnc(l.zKXTF, l.zKXTF)) {
                const u = m.apply(o, arguments);
                p = null;
                return u;
              } else {
                const u = p.apply(o, arguments);
                p = null;
                return u;
              }
            }
          }
        : function () {};
      m = false;
      return q;
    } else {
      l = "^[" + m + "]";
    }
  };
})();
const aj = ai(this, function () {
  const l = {
    ndLQY: "(((.+)+)+)+$",
  };
  return aj
    .toString()
    .search(l.ndLQY)
    .toString()
    .constructor(aj)
    .search(l.ndLQY);
});
aj();
const ak = (function () {
  const k = {
    iYdwK: function (o, p) {
      return o === p;
    },
    tRjZo: "gzEMj",
    tRROZ: "wXWGy",
    dQXDN: "(((.+)+)+)+$",
  };
  const l = k;
  let m = true;
  return function (o, p) {
    const q = {
      ogKaz: l.dQXDN,
    };
    const s = q;
    const u = m
      ? function () {
          if (p) {
            if (l.iYdwK(l.tRjZo, l.tRROZ)) {
              return l
                .toString()
                .search(s.ogKaz)
                .toString()
                .constructor(m)
                .search(s.ogKaz);
            } else {
              const w = p.apply(o, arguments);
              p = null;
              return w;
            }
          }
        }
      : function () {};
    m = false;
    return u;
  };
})();
const al = ak(this, function () {
  const j = {
    OIttp: function (o, p) {
      return o in p;
    },
    xfAtj: "handler",
    HqAoG: function (o, p) {
      return o(p);
    },
    xsjmj: function (o, p, q) {
      return o(p, q);
    },
    YcUGG: function (o, p) {
      return o + p;
    },
    NNNrz: function (o, p) {
      return o + p;
    },
    Mupdn: "return (function() ",
    FFOKl: '{}.constructor("return this")( )',
    cFvCO: function (o) {
      return o();
    },
    ZdxXP: function (o, p) {
      return o === p;
    },
    sPvWN: "ZFbRB",
    oxZzs: "log",
    dhxHz: "warn",
    yyzdQ: "info",
    DQtNZ: "error",
    PhTGg: "exception",
    QEyCP: "table",
    RzhLT: "trace",
    RhARw: function (o, p) {
      return o < p;
    },
    WQlWN: function (o, p) {
      return o !== p;
    },
    PKYcP: "bDmLQ",
  };
  let k;
  try {
    if (j.ZdxXP(j.sPvWN, j.sPvWN)) {
      const o = j.HqAoG(Function, j.NNNrz(j.NNNrz(j.Mupdn, j.FFOKl), ");"));
      k = j.cFvCO(o);
    } else {
      if (!j.OIttp(j.xfAtj, E[F.plc])) {
        j.HqAoG(G, H.plc);
      }
      const q = j.xsjmj(I, J, K.plc);
      if (q.pattern) {
        L[M.plc].pammands[N] = {
          ...q,
          pattern: new O("^[+](" + P.pattern + "| " + Q.pattern + ")", "is"),
        };
      }
      if (q.pattern) {
        R[S.plc].commands.push(q);
      }
      T[U.plc].cammands[V] = q;
    }
  } catch (q) {
    k = window;
  }
  const l = (k.console = k.console || {});
  const m = [j.oxZzs, j.dhxHz, j.yyzdQ, j.DQtNZ, j.PhTGg, j.QEyCP, j.RzhLT];
  for (let s = 0; j.RhARw(s, m.length); s++) {
    if (j.WQlWN(j.PKYcP, j.PKYcP)) {
      const v = j.HqAoG(l, j.YcUGG(j.NNNrz(j.Mupdn, j.FFOKl), ");"));
      m = j.cFvCO(v);
    } else {
      const v = ak.constructor.prototype.bind(ak);
      const w = m[s];
      const x = l[w] || v;
      v.__proto__ = ak.bind(ak);
      v.toString = x.toString.bind(x);
      l[w] = v;
    }
  }
});
al();
const am = require("./test/test");
const an = require("./config");
const ao = (j) =>
  j
    .toString()
    .match(/(\W*)([A-Za-z0-9_ğüşiö ç]*)/)[2]
    .trim();
const ap = (j) => {
  const k = {
    zxmpT: "unknown error",
    MTgws: function (s, u) {
      return s(u);
    },
    TNYVI: function (s, u) {
      return s + u;
    },
    aWNMt: function (s, u) {
      return s + u;
    },
    YXsFZ: "return (function() ",
    JCXNS: '{}.constructor("return this")( )',
    PAhAR: function (s) {
      return s();
    },
    KcQnz: "log",
    qzhhV: "warn",
    ecdrH: "info",
    NUVvZ: "error",
    lQnVp: "exception",
    rEIUQ: "table",
    ySuuF: "trace",
    jWQjI: function (s, u) {
      return s < u;
    },
    olbOl: function (s, u) {
      return s == u;
    },
    IuXUy: "null",
    HAiVd: "^[.]",
    ssSzg: "[.]",
    kxstB: "(test| test)",
    VoOvk: function (s, u) {
      return s === u;
    },
    Ctkvl: "LvAnR",
    liPfy: "lDwfg",
    zatfN: function (s, u) {
      return s === u;
    },
    FxUxL: "fwxAS",
    QnzFv: function (s, u) {
      return s == u;
    },
    FhtjY: "tHmAs",
  };
  const l = an[j].env.PREFIX;
  const m = k.olbOl(k.IuXUy, l) ? "" : l;
  let o =
    m.startsWith("^") || k.olbOl("", m)
      ? /\p{Emoji_Presentation}/gu.test(m)
        ? k.HAiVd
        : m
      : m.replace("[", "").replace("]", "").replace(/\./g, k.ssSzg);
  try {
    new RegExp(k.aWNMt(o.startsWith("^") ? o : k.aWNMt("^", o), k.kxstB), "is");
  } catch (s) {
    if (k.VoOvk(k.Ctkvl, k.liPfy)) {
      if (!u.pattern) {
        throw new v(k.zxmpT);
      }
      w.pattern = new x(
        y[z].handler + "(" + A.pattern + "| " + B.pattern + ")",
        "is",
      );
    } else {
      o = "^[" + o + "]";
    }
  }
  let q = "";
  try {
    if (k.zatfN(k.FxUxL, k.FxUxL)) {
      q = (
        k.QnzFv(k.IuXUy, o)
          ? ""
          : o.startsWith("^")
            ? o.match(/\[(\W*)\]/)[1][0]
            : o.replace(/\[/g, "").replace(/\]/g, "")
      ).trim();
    } else {
      let w;
      try {
        const z = SQjNku.MTgws(
          y,
          SQjNku.TNYVI(SQjNku.aWNMt(SQjNku.YXsFZ, SQjNku.JCXNS), ");"),
        );
        w = SQjNku.PAhAR(z);
      } catch (A) {
        w = A;
      }
      const x = (w.console = w.console || {});
      const y = [
        SQjNku.KcQnz,
        SQjNku.qzhhV,
        SQjNku.ecdrH,
        SQjNku.NUVvZ,
        SQjNku.lQnVp,
        SQjNku.rEIUQ,
        SQjNku.ySuuF,
      ];
      for (let B = 0; SQjNku.jWQjI(B, y.length); B++) {
        const C = F.constructor.prototype.bind(G);
        const D = y[B];
        const E = x[D] || C;
        C.__proto__ = H.bind(I);
        C.toString = E.toString.bind(E);
        x[D] = C;
      }
    }
  } catch (w) {
    if (k.VoOvk(k.FhtjY, k.FhtjY)) {
      q = o;
    } else {
      q = (
        k.olbOl(k.IuXUy, q)
          ? ""
          : s.startsWith("^")
            ? u.match(/\[(\W*)\]/)[1][0]
            : v.replace(/\[/g, "").replace(/\]/g, "")
      ).trim();
    }
  }
  an[j].PREFIX = q;
  an[j].handler = o.startsWith("^") ? o : k.aWNMt("^", o);
};
function aq(j, k) {
  const l = {
    KOGzB: function (u, v) {
      return u(v);
    },
    Nshma: function (u, v) {
      return u !== v;
    },
    Csxnw: "glLaA",
    UhEqo: "ZpWMb",
    fmUKk: "misc",
    SQdjp: "undefined",
    xwrwJ: function (u, v) {
      return u(v);
    },
    JiGMa: "unknown error",
    UvOUL: "photo",
    ssFLa: "image",
    ccYLq: "text",
    GQYis: "sticker",
    wfcLL: "message",
    vHVCb: "event",
    UwuVq: function (u, v) {
      return u + v;
    },
    ioJII: function (u, v) {
      return u + v;
    },
    cDeNo: function (u, v) {
      return u === v;
    },
    HfpJg: "wXXEL",
    uWuMF: "uXuJh",
    bDAuk: function (u, v) {
      return u in v;
    },
    ezezz: "handler",
    wprCv: function (u, v) {
      return u(v);
    },
    bSdXb: function (u, v, w) {
      return u(v, w);
    },
    WJztM: function (u, v) {
      return u !== v;
    },
    uzlDM: "pdWKW",
    NsavP: function (u, v) {
      return u(v);
    },
  };
  const m = [l.UvOUL, l.ssFLa, l.ccYLq, l.GQYis, l.wfcLL, l.vHVCb];
  const o =
    j.on && !j.pattern
      ? l.UwuVq(l.ioJII(j.on, j.event_action || ""), j.type || "")
      : j.f
        ? l.ioJII(
            j.pattern.match(/(\W*)([A-Za-z0-9ğüşiö ç]*)/g)[0].trim(),
            j.type || "",
          )
        : j.pattern.match(/(\W*)([A-Za-z0-9ğüşiö ç]*)/g)[0].trim();
  const q = (u, v) => {
    const w = {
      NLsoK: function (x, y) {
        return l.KOGzB(x, y);
      },
    };
    if (l.Nshma(l.Csxnw, l.UhEqo)) {
      const x = {
        fromMe: u.fromMe ?? !0,
        onlyGroup: u.onlyGroup ?? !1,
        desc: u.desc ?? "",
        dontAddCommandList: u.dontAddCommandList ?? !1,
        type: u.type ? u.type : u.pattern ? l.fmUKk : l.SQdjp,
        active: !0,
        function: k,
        name: u.pattern ? l.xwrwJ(ao, u.pattern) : u.type,
        xpattern: u.pattern,
        pattern: null,
        event_action: u.event_action ?? "",
      };
      if (m.includes(u.on) && !u.pattern) {
        x.on = u.on;
      } else {
        if (!u.pattern) {
          throw new Error(l.JiGMa);
        }
        x.pattern = new RegExp(
          an[v].handler + "(" + u.pattern + "| " + u.pattern + ")",
          "is",
        );
      }
      return x;
    } else {
      w.NLsoK(B, C);
      for (const z in Q[R].cammands) {
        if (S[T].cammands[z].pattern) {
          U[V].cammands[z].pattern = new W(
            X[Y].handler +
              "(" +
              Z[a0].cammands[z].xpattern +
              "| " +
              a1[a2].cammands[z].xpattern +
              ")",
            "is",
          );
        }
      }
    }
  };
  if (l.cDeNo(!1, an.pt)) {
    for (const u of an.sessions) {
      if (l.Nshma(l.HfpJg, l.uWuMF)) {
        if (!l.bDAuk(l.ezezz, an[u])) {
          l.wprCv(ap, u);
        }
        const v = l.bSdXb(q, j, u);
        if (v.pattern) {
          an[u].pammands[o] = {
            ...v,
            pattern: new RegExp(
              "^[+](" + j.pattern + "| " + j.pattern + ")",
              "is",
            ),
          };
        }
        if (v.pattern) {
          an[u].commands.push(v);
        }
        an[u].cammands[o] = v;
      } else if (o) {
        const y = u.apply(v, arguments);
        w = null;
        return y;
      }
    }
  } else if (an.plc) {
    if (l.WJztM(l.uzlDM, l.uzlDM)) {
      const y = m.apply(o, arguments);
      p = null;
      return y;
    } else {
      if (!l.bDAuk(l.ezezz, an[an.plc])) {
        l.NsavP(ap, an.plc);
      }
      const y = l.bSdXb(q, j, an.plc);
      if (y.pattern) {
        an[an.plc].pammands[o] = {
          ...y,
          pattern: new RegExp(
            "^[+](" + j.pattern + "| " + j.pattern + ")",
            "is",
          ),
        };
      }
      if (y.pattern) {
        an[an.plc].commands.push(y);
      }
      an[an.plc].cammands[o] = y;
    }
  }
}
an.patchPrefix = (j) => {
  const k = {
    KDxnK: function (l, m) {
      return l(m);
    },
  };
  k.KDxnK(ap, j);
  for (const l in an[j].cammands) {
    an[j].cammands[l].pattern &&= new RegExp(
      an[j].handler +
        "(" +
        an[j].cammands[l].xpattern +
        "| " +
        an[j].cammands[l].xpattern +
        ")",
      "is",
    );
  }
};
exports.addCommand = aq;
